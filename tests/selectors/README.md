# Selectors

Named CSS/XPath selectors loaded via `cucumber.js` worldParameters.

- `cms-drupal-core-claro.json` - generic Drupal core / Claro admin selectors.
- `ace_editor.json` - Ace Editor editor / formatter / filter selectors.
